{% extends 'base.html.twig' %}

{% block title %}GroupeGalaxies{% endblock %}

{% block body %}
    <h1>GroupeGalaxies</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ groupe_galaxy.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ groupe_galaxy.Nom }}</td>
            </tr>
            <tr>
                <th>Taille</th>
                <td>{{ groupe_galaxy.Taille }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ groupe_galaxy.Description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('groupe_galaxies_index') }}">back to list</a>

    <a href="{{ path('groupe_galaxies_edit', {'id': groupe_galaxy.id}) }}">edit</a>

    {{ include('groupe_galaxies/_delete_form.html.twig') }}
{% endblock %}
