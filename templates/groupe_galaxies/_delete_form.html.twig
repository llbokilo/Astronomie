<form method="post" action="{{ path('groupe_galaxies_delete', {'id': groupe_galaxy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ groupe_galaxy.id) }}">
    <button class="btn">Delete</button>
</form>
