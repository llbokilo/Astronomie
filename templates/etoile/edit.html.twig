{% extends 'base.html.twig' %}

{% block title %}Edit Etoile{% endblock %}

{% block body %}
    <h1>Edit Etoile</h1>

    {{ include('etoile/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('etoile_index') }}">back to list</a>

    {{ include('etoile/_delete_form.html.twig') }}
{% endblock %}
