<form method="post" action="{{ path('satellite_delete', {'id': satellite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ satellite.id) }}">
    <button class="btn">Delete</button>
</form>
