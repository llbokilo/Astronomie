<form method="post" action="{{ path('systeme_planetaire_delete', {'id': systeme_planetaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ systeme_planetaire.id) }}">
    <button class="btn">Delete</button>
</form>
