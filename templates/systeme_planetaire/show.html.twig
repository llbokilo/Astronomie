{% extends 'base.html.twig' %}

{% block title %}SystemePlanetaire{% endblock %}

{% block body %}
    <h1>SystemePlanetaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ systeme_planetaire.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ systeme_planetaire.Nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ systeme_planetaire.Description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('systeme_planetaire_index') }}">back to list</a>

    <a href="{{ path('systeme_planetaire_edit', {'id': systeme_planetaire.id}) }}">edit</a>

    {{ include('systeme_planetaire/_delete_form.html.twig') }}
{% endblock %}
