{% extends 'base.html.twig' %}

{% block title %}SuperamasGalaxies index{% endblock %}

{% block body %}
    <h1>SuperamasGalaxies index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Taille</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for superamas_galaxy in superamas_galaxies %}
            <tr>
                <td>{{ superamas_galaxy.id }}</td>
                <td>{{ superamas_galaxy.Nom }}</td>
                <td>{{ superamas_galaxy.Taille }}</td>
                <td>{{ superamas_galaxy.Description }}</td>
                <td>
                    <a href="{{ path('superamas_galaxies_show', {'id': superamas_galaxy.id}) }}">show</a>
                    <a href="{{ path('superamas_galaxies_edit', {'id': superamas_galaxy.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('superamas_galaxies_new') }}">Create new</a>
{% endblock %}
