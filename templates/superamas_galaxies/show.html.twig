{% extends 'base.html.twig' %}

{% block title %}SuperamasGalaxies{% endblock %}

{% block body %}
    <h1>SuperamasGalaxies</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ superamas_galaxy.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ superamas_galaxy.Nom }}</td>
            </tr>
            <tr>
                <th>Taille</th>
                <td>{{ superamas_galaxy.Taille }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ superamas_galaxy.Description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('superamas_galaxies_index') }}">back to list</a>

    <a href="{{ path('superamas_galaxies_edit', {'id': superamas_galaxy.id}) }}">edit</a>

    {{ include('superamas_galaxies/_delete_form.html.twig') }}
{% endblock %}
