{% extends 'base.html.twig' %}

{% block title %}Galaxie{% endblock %}

{% block body %}
    <h1>Galaxie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ galaxie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ galaxie.Nom }}</td>
            </tr>
            <tr>
                <th>Taille</th>
                <td>{{ galaxie.Taille }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ galaxie.Description }}</td>
            </tr>
            <tr>
                <th>Diametre</th>
                <td>{{ galaxie.Diametre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('galaxie_index') }}">back to list</a>

    <a href="{{ path('galaxie_edit', {'id': galaxie.id}) }}">edit</a>

    {{ include('galaxie/_delete_form.html.twig') }}
{% endblock %}
